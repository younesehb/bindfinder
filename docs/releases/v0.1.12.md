# bindfinder v0.1.12

## Highlights

- adds `bindfinder config validate`
- makes invalid config errors point to the config file and failing field more clearly
- makes `bindfinder doctor` concise by default, with `--snippet` for the full generated integration

## Assets

- `install.sh`
- `bindfinder-0.1.12-x86_64-unknown-linux-musl.tar.gz`
- `bindfinder-0.1.12-aarch64-apple-darwin.tar.gz`
