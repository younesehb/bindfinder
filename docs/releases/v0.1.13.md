# bindfinder v0.1.13

## Highlights

- improves installer output with clearer progress steps
- fixes installer shortcut hints to match the current `Ctrl-]` defaults
- catches mixed shell/tmux key syntax like `ctrl-]` vs `C-]` with explicit config validation hints

## Assets

- `install.sh`
- `bindfinder-0.1.13-x86_64-unknown-linux-musl.tar.gz`
- `bindfinder-0.1.13-aarch64-apple-darwin.tar.gz`
