# v0.1.6

This release cleans up the installer output.

Highlights:

- installer output is quieter and focuses on the result
- post-install guidance now points users to `bindfinder`, `bindfinder --help`, and the docs URL
- noisy internal download details are no longer shown during normal install

Assets:

- `install.sh`
- `bindfinder-0.1.6-x86_64-unknown-linux-musl.tar.gz`
- `bindfinder-0.1.6-aarch64-apple-darwin.tar.gz`
