## Improvements

- remove `integration.launch_key` from the config
- use the same `ctrl-...]` syntax in YAML for both shell and tmux bindings
- translate tmux keys to tmux syntax internally when rendering tmux bindings
