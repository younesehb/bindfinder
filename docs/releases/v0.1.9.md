# bindfinder v0.1.9

## Highlights

- fixes the GitHub release workflow so both Linux and macOS assets upload reliably
- keeps the installer flow introduced in `v0.1.8`, including the default `denisidoro/cheats` import when `git` is available

## Assets

- `install.sh`
- `bindfinder-0.1.9-x86_64-unknown-linux-musl.tar.gz`
- `bindfinder-0.1.9-aarch64-apple-darwin.tar.gz`
